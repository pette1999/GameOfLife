#include <iostream>
#include "game.h"

using namespace std;

int main(int argc, char** argv)
{
    // classic c;
    // c.classicMode();

    // doughnut d;
    // d.doughnutMode();

    // mirror m;
    // m.mirrorMode();

    game g;
    g.gameStart();

    // grid g;
    // g.generateRandomGrid(30, 40, 0.43);
    // g.printGrid();
    // g.printNewGrid();
}